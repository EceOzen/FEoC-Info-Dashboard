# Schema validation catches structural problems, but not scientific ones:
# a physically wrong panel can still be schema-valid.
#
# Rule of thumb: the last matching pattern wins and owners do NOT merge across
# lines, so every line below lists @EceOzen explicitly (code review) plus the
# domain scientist (scientific review) where relevant.

# Default: Ece reviews everything.
*                              @EceOzen

# GHG: code review (Ece) + scientific review (Chris).
# TODO: replace CHRIS_HANDLE with his real GitHub username, then uncomment.
/methods/ghg/                  @EceOzen
/site/panels/ghg/              @EceOzen
# /methods/ghg/                @EceOzen @CHRIS_HANDLE
# /site/panels/ghg/            @EceOzen @CHRIS_HANDLE

# Volcanic: code review (Ece) + scientific review (Dominik).
# TODO: replace DOMINIK_HANDLE with his real GitHub username, then uncomment.
/methods/volcanic/             @EceOzen
/site/panels/volcanic/         @EceOzen
# /methods/volcanic/           @EceOzen @DOMINIK_HANDLE
# /site/panels/volcanic/       @EceOzen @DOMINIK_HANDLE

# Contract changes affect both domains: Ece + both scientists.
# TODO: once handles are known, use:
# /schema/panel.schema.json    @EceOzen @CHRIS_HANDLE @DOMINIK_HANDLE
/schema/panel.schema.json      @EceOzen

# Pipeline code, CI and dashboard: Ece only (covered by the * line above,
# listed here for clarity).
/feoc_panels/                  @EceOzen
/scripts/                      @EceOzen
/.github/                      @EceOzen
